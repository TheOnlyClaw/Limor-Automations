{
  "name": "limor-automations",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm -w apps/web run dev",
    "dev:web": "npm -w apps/web run dev",
    "build": "npm -w apps/web run build",
    "lint": "npm -w apps/web run lint --if-present",
    "typecheck": "npm -w apps/web exec tsc -b",
    "spec": "opsx-kit"
  },
  "devDependencies": {
    "openspec-kit": "^0.2.0"
  }
}
